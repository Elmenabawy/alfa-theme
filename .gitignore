# Dependencies
node_modules/

# Build intermediates (generated by scripts/build-css.mjs)
src/assets/styles/.app.compiled.css

# Compiled CSS is generated on build/push; keep it out of source control.
# (Remove this line if your deploy pipeline expects a committed app.css.)
src/assets/styles/app.css

# OS / editor cruft
.DS_Store
Thumbs.db
*.log
npm-debug.log*
.vscode/
.idea/

# Salla CLI local state
.salla/

# Local static design preview (not part of the shipped theme)
preview/
