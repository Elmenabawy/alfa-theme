// home.js — home-page entry bundle (asset('home.js')).
// Home behaviors are handled globally by app.js (initPerPage runs on load and
// on salla `page::changed`). This entry exists so the home bundle resolves and
// gives you a place for home-only logic later.
export {};
